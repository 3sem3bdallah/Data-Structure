# Two pointers: one input, opposite ends

def fn(arr):
    left = ans = 0
    right = len(arr) - 1

    while left < right:
        # do some logic here with left and right
        CONDITION = False
        if CONDITION:
            left += 1
        else:
            right -= 1
    
    return ans
